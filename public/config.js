// `npm run build` replaces this file in dist with Vercel's runtime build variables.
// For local development, add values here only in an untracked local override if needed.
window.TODO_CONFIG = window.TODO_CONFIG || { supabaseUrl: '', supabaseAnonKey: '' };
